{
  "scripts": {
    "backend:serve": "cd backend && npm run serve",
    "frontend:dev": "cd frontend && npm run dev",
    "dev": "npx concurrently \"npm run backend:serve\" \"npm run frontend:dev\""
  },
  "devDependencies": {
    "autoprefixer": "^10.4.24",
    "concurrently": "^8.2.2"
  }
}